/**
 * Gmail — Phase 2.
 *
 * TODO: Implement Gmail API with OAuth2
 *   1. Create OAuth2 credentials in Google Cloud Console (same project as Calendar)
 *   2. Request scopes: gmail.readonly, gmail.labels
 *   3. Store GMAIL_REFRESH_TOKEN in .env after auth flow
 *   4. Fetch: unread count, senders with action items, threads needing response
 *   5. Return typed GmailContext
 */

export async function fetchGmail(): Promise<null> {
  return null;
}
