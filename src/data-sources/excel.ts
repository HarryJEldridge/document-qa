/**
 * Excel model parser — Phase 2.
 *
 * TODO: Implement Excel parsing
 *   1. Set EXCEL_FILE_PATH in .env to your financial model / tracker
 *   2. Install: npm install xlsx
 *   3. Read configured sheet names and named ranges
 *   4. Extract KPIs, targets, actuals, variance
 *   5. Return typed ExcelContext
 */

export async function fetchExcel(): Promise<null> {
  return null;
}
